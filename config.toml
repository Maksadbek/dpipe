[inputs]
        [inputs.csv]
                file = "testdata/hotels.csv"

[outputs]
        [outputs.json]
                file = "testdata/hotels.json"
        [outputs.xml]
                file = "testdata/hotels.xml"

[filters]
        [filters.range]
                enabled = false
                field = "stars"
                min = 0
                max = 0
        [filters.url]
                field = "uri"
[aggregators]
        [aggregators.group]
        field = "name"

        [aggregators.sort]
        field = "stars"
